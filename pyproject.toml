[project]
name = "airflow-data-pipelines"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apache-airflow[gcp]>=3.1.8",
    "pydantic>=2.12.5",
]
